# Dependencies
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# Build outputs
dist/
build/
*.egg-info/
target/

# Smoke test temp dirs (created under repo root so typebox resolves)
.smoke-ext-tmp-*

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Environment / secrets (PREVENT: never commit)
.env
.env.local
*.local

# Logs
*.log
logs/

# ithacus runtime state (the resident .pi/ithacus folder travels with repos,
# but ignore any stray local state in THIS repo)
.pi/ithacus/
.pi/

# Scratch / audit probe scripts
tmp/

# DevGate framework reference clone (vendored-net-new tools live in scripts/;
# this clone is reference-only and not part of the ithacus npm package).
DevGate-Agentic-Framework/
